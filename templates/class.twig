<?php

namespace {{config.namespace}};

use {{config.testcase}} as TestCaseImplementation;
use {{class.namespace}}\{{class.name}};

/**
 * this is an automatically generated skeleton for testing {{class.name}}
 * @todo actually test
 **/
{% if class.abstract %}abstract {% endif %}class {{class.name}}Test extends TestCaseImplementation
{
    /**
     * @return {{class.name}}
     * @todo make sure the construction works as expected
     **/
{% if class.abstract %}
    abstract protected function getInstance ();
{% else %}
    protected function getInstance ()
    {
        return new {{class.name}}{% include 'function.twig' with {'function': class.constructor} only %};
    }
{% endif %}
{% include 'test-functions.twig' with {'class': class, 'classes': classes} only %}
}
